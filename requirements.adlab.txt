# AdLab Viral Clip Factory Requirements
# Extends ClipsAI with minimal additional dependencies

# Core dependencies (if not already in clipsai)
# Most are already included in clipsai's setup.py

# CLI and UI
typer>=0.9.0
rich>=13.7.0

# LLM Integration
anthropic>=0.18.0

# Configuration
pyyaml>=6.0

# Note: The following are already included in clipsai:
# - torch (for GPU/CPU processing)
# - whisperx (for transcription)
# - numpy
# - opencv-python (for video processing)
# - pyannote.audio (for diarization)
# - sentence-transformers (for embeddings)
# - nltk

# Total new dependencies: ~5 packages
# Well under the <20 constraint
